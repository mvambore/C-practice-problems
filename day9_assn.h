#include <stdio.h>
#include <stdlib.h>
#include<string.h>


void display_contents(char *file_name); // function to display the contents of a file

int file_copy(char *source, char *destination); // function to copy the contents of one file to another file

int count_chars_spaces_line(char *file_name); // this counts the number of characters, spaces and lines in a given file

int file_odd_even(char *file_name); // function for counting the odd numbers and even numbers

